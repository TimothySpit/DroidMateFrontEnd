This is a Folder to test and play around with Java Webfrontends using bootstrap. It was made following the instructions on "http://www.oracle.com/webfolder/technetwork/tutorials/obe/java/basic_app_embedded_tomcat/basic_app-tomcat-embedded.html#section6" .

To build it, you need the a JDK of 1.8 (check with java -version) and maven (3.3.9). 
Navigate into the source directory of the project and type 'mvn clean compile' , and then 'mvn package'.
Then, navigate into the new "target/" folder.
Then type 'java -jar employees-app-1.0-SNAPSHOT-jar-with-dependencies.jar'
If it works, you should now be able to open the created website on "http://localhost:8080" in your browser.

The project can also be imported into Eclipse or Intellij. 

You can play around with the settings, and try to create new functions, in order to understand the system. you then need to recompile the project using steps above.
